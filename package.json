{
  "name": "workbench-hr-preview",
  "private": true,
  "description": "Local preview server with SQLite-backed demo auth for Workbench HR static pages.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.2"
  }
}
