{
  "name": "workbench-hr-root",
  "private": true,
  "description": "Convenience scripts; install dependencies in ./web",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web"
  }
}
